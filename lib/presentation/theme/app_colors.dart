
import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF560bac);
  static const gradprimary = LinearGradient(
    colors: [
      Color(0xFF560bac),
      Color(0xFFAE00FF),
    ],
    begin: Alignment.centerLeft,
    end: Alignment.centerRight,
  );
  
}