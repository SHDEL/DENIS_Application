class UserEntity {
  String ? id;
  String ? username;
  String ? email;
  String ? password;
  String ? role;

  UserEntity({
    this.id,
    this.username,
    this.email,
    this.password,
    this.role,
  });
}